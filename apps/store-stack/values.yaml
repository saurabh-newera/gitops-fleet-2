# Default values for store-stack
# These are baseline defaults - override in versions/*.yaml and clusters/*.yaml

nginx-service:
  namespace: default
  replicaCount: 1
  image:
    repository: nginx
    tag: "1.27.0"
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 80
  resources:
    limits:
      cpu: "150m"
      memory: "158Mi"
    requests:
      cpu: "60m"
      memory: "74Mi"
  env:
    APP_ENV: "production"

httpbin-service:
  namespace: default
  replicaCount: 1
  image:
    repository: kennethreitz/httpbin
    tag: "latest"
    pullPolicy: IfNotPresent
  service:
    type: NodePort
    port: 80
    nodePort: 30080
  resources:
    limits:
      cpu: "140m"
      memory: "148Mi"
    requests:
      cpu: "50m"
      memory: "64Mi"
  env:
    APP_ENV: "production"
    LOG_LEVEL: "info"

redis-service:
  namespace: default
  replicaCount: 1
  image:
    repository: redis
    tag: "7.2.0"
    pullPolicy: IfNotPresent
  resources:
    limits:
      cpu: "250m"
      memory: "258Mi"
    requests:
      cpu: "100m"
      memory: "174Mi"
  service:
    type: ClusterIP
    port: 6379
  env:
    REDIS_MAXMEMORY: "228mb"

postgres:
  namespace: default
  replicaCount: 1
  image:
    repository: postgres
    tag: "15.4"
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 5432
  storage:
    size: "10Gi"
    storageClass: "standard"
  config:
    database: "appdb"
    user: "appuser"
